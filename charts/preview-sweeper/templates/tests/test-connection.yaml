apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "preview-sweeper.fullname" . }}-test-connection"
  labels:
    {{- include "preview-sweeper.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "preview-sweeper.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
