apiVersion: v1
kind: Service
metadata:
  labels:
    control-plane: preview-sweeper-controller
    app.kubernetes.io/name: preview-sweeper
    app.kubernetes.io/managed-by: kustomize
  name: preview-sweeper-controller-metrics-service
  namespace: system
spec:
  ports:
  - name: https
    port: 8443
    protocol: TCP
    targetPort: 8443
  selector:
    control-plane: preview-sweeper-controller
    app.kubernetes.io/name: preview-sweeper
